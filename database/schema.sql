create table people (
        id integer primary key,
        firstname varchar(40),
        lastname varchar(40),
        email varchar(60),
        address1 varchar(40),
        address2 varchar(40),
        city varchar(40),
        province varchar(30),
        postcode varchar(10),
        country varchar(30) 
);
